import AdminPanel from "../../components/AdminPanel";
export const metadata = { title: "Research Administration", robots: { index: false, follow: false } };
export default function AdminPage(){ return <AdminPanel/>; }
